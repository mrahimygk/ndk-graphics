<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.mrahimy.ndkgraphics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NdkGraphics">
        <activity
            android:name=".pages.SineActivity"
            android:label="@string/sine" />
        <activity
            android:name=".pages.LineActivity"
            android:label="@string/sine" />

        <activity
            android:name=".pages.RandomActivity"
            android:label="@string/random" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>